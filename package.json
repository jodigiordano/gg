{
  "name": "@dataflows",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "license": "See LICENSE",
  "engines": {
    "node": ">= 21.6",
    "npm": ">= 10.2"
  },
  "scripts": {
    "compile": "npm run compile --workspaces",
    "lint": "npm run lint --workspaces",
    "maintenance": "npm update --workspaces",
    "dev": "concurrently --kill-others --names spec,simulator,frontend \"(cd spec && npm run dev)\" \"(cd simulator && npm run dev)\" \"(cd frontend && npm run dev)\""
  },
  "devDependencies": {
    "concurrently": "8.x"
  },
  "workspaces": [
    "spec",
    "simulator",
    "frontend"
  ]
}
