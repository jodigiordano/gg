{
  "$id": "definitions",
  "$defs": {
    "textFont": {
      "$id": "TextFont",
      "type": "string",
      "enum": ["text", "sketch", "code"]
    },
    "textAlign": {
      "$id": "TextAlign",
      "type": "string",
      "enum": ["left", "center", "right"]
    },
    "borderPattern": {
      "$id": "BorderPattern",
      "type": "string",
      "enum": ["light", "solid", "dotted"]
    },
    "borderEdge": {
      "$id": "BorderEdge",
      "type": "string",
      "enum": ["round", "straight"]
    }
  }
}
